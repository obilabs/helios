# Frontend Configuration

# API URL Configuration
# =====================
# For LOCAL development only (direct access on same machine):
#   VITE_API_URL=http://localhost:3001
#
# For REMOTE access via nginx proxy (recommended for production/testing):
#   VITE_API_URL=
#   (Leave empty to use relative URLs through nginx reverse proxy)
#
# The nginx proxy routes /api/* to the backend automatically.
# Access the app via http://<server-ip>:80 when using nginx.
VITE_API_URL=

# Theme Selection (optional)
# Options: purple (default), blue, green, gray
VITE_THEME=purple

# Application Name (optional)
VITE_APP_NAME="Helios Admin Portal"

# Organization Name (optional - can be set dynamically)
VITE_ORG_NAME=""

# Enable Debug Mode (optional)
VITE_DEBUG=false